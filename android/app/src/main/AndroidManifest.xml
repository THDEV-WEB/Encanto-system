<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- REF-CAP-01 · Onda 4 (D5): volta do login Google nativo (Browser do sistema + deep link).
                 Esquema precisa casar EXATAMENTE com DEEP_LINK_LOGIN em src/services/AuthService.js e com
                 a entrada correspondente no allow-list de Redirect URLs do Supabase Auth. launchMode
                 singleTask (acima, ja existente) garante que o app nao reinicia ao receber o deep link -
                 o listener appUrlOpen registrado em JS continua vivo pra captura-lo. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="br.com.valionsistemas.encanto" android:host="login-callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- REF-CAP-01 · Onda 4: busca de endereco (src/address/hooks/useAddressSearch.js) usa
         navigator.geolocation.getCurrentPosition sem nenhum plugin Capacitor dedicado — a Bridge nativa
         do Capacitor ja intercepta o prompt de geolocalizacao da WebView (onGeolocationPermissionsShowPrompt)
         e pede a permissao do Android automaticamente, DESDE QUE ela exista aqui no manifest. Nenhuma
         mudanca de codigo JS necessaria. COARSE + FINE (o Android sempre concede as duas juntas quando
         FINE e' aceita; COARSE sozinha e' o fallback se o usuario negar FINE). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>
